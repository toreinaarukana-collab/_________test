versions.push_back({
    .GAME_VERSION       = "2024082600-012",

    .DLL_CODE_SIZE      = 0x0B13600,
    .DLL_ENTRYPOINT     = 0x09B323C,
    .DLL_IMAGE_SIZE     = 0x78B4000,

    .SOFT_REV_PATCH     = base + 0x081B569,
    .INPUT_POLL_FN      = base + 0x09217D0,
    .ARENA_PHASE_PATCH  = base + 0x04932D0,
    .MUSIC_SELECT_CTOR  = base + 0x04F2620,
    .DAN_SELECT_CTOR    = base + 0x07850A0,
    .SCENE_DTOR         = base + 0x0836010,
    .STAGE_RESULT_FN    = base + 0x07F4740,
    .RESET_STATE_FN     = base + 0x049DB40,
    .MSELECT_GENRE_A    = base + 0x04F1DB6,
    .MSELECT_GENRE_B    = base + 0x04F1CDC,
    .MSELECT_GENRE_C    = base + 0x04F1C50,
    .TEXT_RENDER_FN     = base + 0x04DE5D0,
    .TEXT_INIT_FN       = base + 0x0305640,
    .RESULT_ARTIST_FN   = base + 0x07E6572,
    .LOAD_CHART_FN_A    = base + 0x06DF150,
    .LOAD_CHART_FN_B    = base + 0x06DF040,
    .REMAP_INDEX_FN     = base + 0x06DF1A0,
    .SCORE_INVALID_FN   = base + 0x06E57A0,
    .XRPC_APPLY_FN      = base + 0x0957800,
    .REG_DISPATCH_FN    = base + 0x07E10B0,
    .REG_PATCH_ADDR     = base + 0x07E1BE0,
    .DAN_SAVE_FN        = base + 0x04A4B90,
    .EAAPPLI_SAVE_FN    = base + 0x04A9400,
    .MDATA_LOAD_FN      = base + 0x07FCA20,
    .IS_BTN_DOWN_FN_A   = base + 0x06D6CBD,
    .IS_BTN_DOWN_FN_B   = base + 0x06D6D5D,
    .AUTO_BEAM_PATCH    = base + 0x07D354B,
    .CARD_OUT_VFUNC     = base + 0x0BE5720,
    .RENDERER_PATCH     = base + 0x04B3F7F,
    .APPLY_RANDOM_FN    = base + 0x06DB4A0,
    .DARK_MODE_PATCH    = base + 0x07CFEA7,
    .MEASURE_PATCH      = base + 0x06DBD96,
    .BPM_BAR_PATCH      = base + 0x07CF288,
    .BPM_BAR_PATCH_JMP  = 0x00000BA,
    .PLAY_FIELD_LOAD    = base + 0x06D48C0,
    .RETRY_CHECK_A      = base + 0x07DC065,
    .RETRY_CHECK_B      = base + 0x07DC077,
    .GHOST_TARGET_FN    = base + 0x07D3DF8,
    .GRAPH_TARGET_FN    = base + 0x08EC505,
    .GRAPH_CONDITION    = base + 0x08EC4BD,
    .FAIL_ANIMATION_FN  = base + 0x07DC7DB,
    .FAIL_PLAY_SFX_FN   = base + 0x07DC805,
    .FAIL_DURATION_JMP  = base + 0x07DC235,
    .GET_MUSIC_DATA     = base + 0x081E170,
    .GET_APP_CONFIG     = base + 0x0803F70,
    .WNDPROC_FN         = base + 0x0805470,
    .TIMING_HOOK_FN     = base + 0x06D9620,
    .ATTRACT_SELECT_FN  = base + 0x075E030,

    .GAME_MODEL         = base + 0x0E98990,
    .GAME_STATE         = base + 0x6B1DD40,
    .INPUT_ADDR         = base + 0x6FDCEFC,
    .SCORES_P1          = base + 0x2B3C084,
    .SCORES_P2          = base + 0x486ADDC,
    .RIVAL_SCORES_P1    = base + 0x328F128,
    .RIVAL_SCORES_P2    = base + 0x4FBDE80,
    .D3D9_DEVICE        = base + 0x6B1E980,
    .AUTO_PLAY          = base + 0x68BF73C,
    .RANDOM_COLUMNS     = base + 0x661A520,
    .IN_GAMEPLAY        = base + 0x69EFC14,
    .DEAD_STATE         = base + 0x69EF750,
    .SCORE_CURRENT_P1   = base + 0x6618470,
    .NOTE_CURRENT_P1    = base + 0x661847C,
    .NOTE_TOTAL_P1      = base + 0x6618480,
    .SCORE_CURRENT_P2   = base + 0x66187D8,
    .NOTE_CURRENT_P2    = base + 0x66187E4,
    .NOTE_TOTAL_P2      = base + 0x66187E8,
    .PACEMAKER_TARGET   = base + 0x6618EAC,
    .CURRENT_SCORE_PB   = base + 0x69EFF24,
    .PACEMAKER_TYPE_ID  = base + 0x69EFF50
});